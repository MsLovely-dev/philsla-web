# Implementation Plan — Maintenance Table: List of DepEd SHS (demo-ready)

> **For agentic workers:** REQUIRED SUB-SKILL: use `superpowers:subagent-driven-development` or `superpowers:executing-plans` once this plan is human-approved. Steps use checkbox (`- [ ]`) syntax. Aligns to `BUILD_PLAN.md`: **demo-ready by Friday, not 100% completion.**

**Owner:** JP Mayordo (JP.Mayordo) · **Date:** 2026-08-06 · **Branch:** `jp.mayordo/deped-shs` · **Track:** 🟢 committed

**Status:** ✅ Executed 2026-08-06 — all phases green (see `implement/jp.mayordo.implement.md`). Pending: commit + PR.

**Goal:** Make the List of DepEd SHS maintenance table demo-solid. Its backend, service, and screen are already wired (PR #44), so this story = verify end-to-end + close the one gap vs. the Universities slice (missing screen test coverage). **Zero behavioural change** to shipped code.

**Architecture:** Frontend-test-only + verification. No Django/service/screen changes. Add a Vitest component test and a Playwright e2e smoke, each mirroring a *working* reference. Run tests after each artifact; never claim green without running.

**Tech Stack:** React 19 / TS 5.8 / Vite 6 / Vitest 4 / Playwright (frontend); Django 5.2 / DRF (backend, verify-only). Local dev DB SQLite.

## Global constraints

- Additive, test-only. Do **not** modify `apps/schools`, `backendSchoolService.ts`, or `SchoolsListMaintenance.tsx`.
- Mirror **working** references only: `backendSchoolService.test.ts` (service-test style) and `maintenance-universities.spec.ts` (e2e style). Do **not** copy the broken `UniversitiesListMaintenance.test.tsx`.
- Assert against the real screen's actual copy/selectors (verified in `SchoolsListMaintenance.tsx`): visible button `Add New School`; icon buttons `title="Edit School Details"` / `title="Remove School"`; empty state `No schools match your search and filter criteria.`; modal save `Save School Record`; confirm dialog label `Agree`.
- Two-backend university duplication is explicitly **out of scope** (tracked as tech debt, see note below).

## Approval gate

- [x] Direction confirmed: **follow the Build Plan (demo-ready)**, not full 100% / no backend reconciliation.
- [x] Scope: test-parity + verify only, no production code changes.

---

## Phase A — Verify the shipped Schools slice

Reference: `backend/apps/schools/tests.py`, `frontend/src/services/backendSchoolService.test.ts`.

- [x] `python manage.py test apps.schools --settings=config.settings.test` → **5 passed**, unchanged.
- [x] `npx vitest run src/services/backendSchoolService.test.ts` → **4 passed**, unchanged.
- [x] Confirmed `SchoolsListMaintenance.tsx` already uses `schoolService` + `ServiceResult` + `ConfirmationDialog` (no rewire needed).

## Phase B — Screen component test (the primary gap)

Reference: structure from `UniversitiesListMaintenance.test.tsx` (mock the service singleton, `MemoryRouter`, `userEvent`) but written against the real non-paginated `schoolService`.

- [ ] Create `frontend/src/pages/admin/maintenance/SchoolsListMaintenance.test.tsx`:
  - `vi.spyOn(schoolService, 'listSchools')` → `serviceSuccess([...])`; assert rows render (name + `SCH-#####`).
  - Empty list → asserts `No schools match your search and filter criteria.`
  - Add: click `Add New School`, fill name + capacity, submit `Save School Record`; assert `createSchool` called with payload (no `code`) and the row renders.
  - Validation error: `createSchool` → `validationError(...)`; assert modal stays open + error banner shows the message.
  - Delete: `title="Remove School"` → `ConfirmationDialog` → `Agree`; assert `deleteSchool` called and row disappears.
- [x] `npx vitest run src/pages/admin/maintenance/SchoolsListMaintenance.test.tsx` → **5 passed**.

## Phase C — e2e smoke (list → add → edit → delete)

Reference: `frontend/e2e/maintenance-universities.spec.ts` (prototype/Mock mode, single page session, `SyntheticSession` SYSTEM_ADMIN via `addInitScript` on `philsa_user`).

- [ ] Create `frontend/e2e/maintenance-schools.spec.ts`:
  - Seed SYSTEM_ADMIN session; `goto('/admin/maintenance/schools')`; assert heading `List of Schools` + empty state.
  - Add a school (name + capacity); assert row appears, empty state clears.
  - Edit via `title="Edit School Details"` → rename → `Save School Record`; assert rename round-trips.
  - Delete via `title="Remove School"` → `alertdialog` → `Agree`; assert row gone + empty state returns.
  - No course drill-down; no `page.reload()` (Mock resets on reload).
- [x] `npx playwright test e2e/maintenance-schools.spec.ts` → **1 passed** (run with `VITE_AUTH_SERVICE_MODE=prototype` to override the local `.env.local` backend-mode setting; the pre-existing universities e2e requires the same override).

## Definition of done

- [x] `SchoolsListMaintenance.test.tsx` green (5); `backendSchoolService.test.ts` still green (4).
- [x] `maintenance-schools.spec.ts` green (1, prototype mode; `.env.local` override caveat disclosed).
- [x] `apps.schools` backend tests still green (5/5), unchanged.
- [x] `npm run lint` (tsc) reports no new errors in the added files (36 pre-existing repo-wide, unrelated).
- [x] Logged in `docs/superpowers/jp.mayordo/implement/jp.mayordo.implement.md` (DepEd SHS section) referencing this plan.
- [ ] PR opened against `main` for review (no direct pushes). — **pending your go-ahead**

## Risks / notes

- Test-only + additive → the demo path cannot regress from this story.
- **Tech-debt flag (out of scope here):** two live University backends exist — `apps.universities` (`/api/v1/universities/`, simple, consumed by the frontend) and `apps.configuration` admin registry (`/api/v1/configuration/admin/universities/`, paginated + versioned + Module-38 scoped, documented as canonical but not consumed by any frontend). The orphaned `UniversitiesListMaintenance.test.tsx` was written for the configuration contract and currently fails. Recommend a separate story to reconcile these; not touched here per the Build Plan's demo-ready scope.
- Playwright browser binaries may need provisioning (`npx playwright install`); Phase C green is reported only from an actual run.
